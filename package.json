{
  "name": "lmi-jobstories-server",
  "version": "1.0.0",
  "dependencies": {
    "ajv": "^6.5.4",
    "express": "^4.16.4"
  },
  "engines": {
    "node": "10.11.0",
    "npm": "6.4.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
